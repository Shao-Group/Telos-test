"""Paper reproduction package: CLI, registry, parity, benchmark/eval/plot.

Train/predict go through ``telos_repro.backend`` → product ``telos``.
Shared helpers are imported from ``telos.*``; paper-only modules live here.
See ``docs/ARCHITECTURE.md``.
"""

from __future__ import annotations

__version__ = "0.1.0"
