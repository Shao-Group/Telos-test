# Optional local overrides for scripts/parity/*.sh (gitignored).
# Copy to local.env and edit.

# TELOS_SRC=/absolute/or/relative/path/to/Telos/src
# TELOS_BUNDLES_ROOT=/path/to/data/bundles
# GFFCOMPARE=gffcompare
# REF_RUNS_ROOT=/path/to/reference/runs
# CONDA_ENV=my-telos-env
# TOTAL_CPUS=64
